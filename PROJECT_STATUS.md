# PROJECT_STATUS

最終更新: 2026-09-10（実運用フィードバック：予定一覧・カテゴリ順を実装）

## 1. アプリの目的

- Android Chromeでホーム画面に追加して使う、個人用の筋トレ消化チェックPWA。
- 利用者は週メニューに沿ってジムでのトレーニングを消化し、実施記録・設定履歴・負荷推移を確認する。
- 現在の主要機能は、設定、週の実行、実施履歴保存、設定履歴、負荷分析である。

## 2. 現在の開発状況

### 完了している機能

- React + TypeScript + Vite PWA、IndexedDBへの端末内保存。
- 種目、カテゴリ、実施項目、週メニューの登録・編集・非表示化／再表示。
- 月曜始まりの週メニュー、実行、追加トレーニング、実施取消、設定登録、メモ2継続。
- 実行取消は現在週（月曜開始）の最新実行記録だけに限定する。先週以前の実績は変更・取消できない。
- 種目ごとの回数型／時間型、重量有無、重量係数（×1／×2）、自重換算係数、秒間負荷係数。
- プロフィール（体重初期値66kg、身長、年齢、性別）。新しい実施履歴には実行時体重を保存する。
- 同日・同一実施項目の設定履歴は最新1件だけを保持する。
- 設定履歴を実行画面から直接表示し、戻る操作で実行画面へ復帰する導線。
- 履歴・分析画面：実施総重量／総トレーニング負荷（参考）、週／月／四半期／半年／年、全体／カテゴリ別／カテゴリ詳細（部位別・種目別）の折れ線グラフ。
- 設定の「分析」と「設定履歴」は別画面。設定履歴はカテゴリ→実施項目→日付一覧で確認する。
- 種目一覧・実施項目一覧のカテゴリ絞り込みと非表示表示、カテゴリ一覧の非表示表示は、編集画面から戻っても維持する。
- 設定 → データ管理で、状態全体を日付付きJSONとしてバックアップできる。JSON復元はv4形式・必須配列・プロフィールを確認し、確認後に端末内データ全体を置換する。
- `.github/workflows/deploy-pages.yml` を追加。`main` へのpushでGitHub Actionsが本番ビルドし、GitHub Pagesへ公開する。
- GitHub Pagesの`/my-app/`配下でJS/CSSを正しく参照するため、`vite.config.ts` に`base: './'`を設定した。
- データスキーマをv5へ移行。部位に「腕」を追加し、既存の「体幹・腕」カテゴリにも自動追加する。肩は既存のまま。
- カテゴリ一覧で↑／↓により登録順を保存できる。各カテゴリ順表示・カテゴリ選択肢はこの順を使用する。
- トップにカテゴリ表示／推奨曜日表示の切替を追加。推奨曜日表示は過去推奨日の未実施を先頭にし、任意・未来推奨は表示しない。実施済表示は任意、初期オフ。
- トップの実施数は予定単位で表示する。今日=`今日推奨の完了数/今日推奨予定数`、今週=`週メニュー予定の完了数/全予定数`。追加トレーニングは含めない。
- カテゴリ画面に実施済表示チェックを追加し、初期は未実施のみ表示する。
- グラフは直近12区間、空白期間は0、当期は「途中」と表示。縦軸は最大値・中央値・0を表示する。
- トップ下部に今週の総トレーニング負荷、完了週の平均負荷、ACWR（直近完了週÷その前4週平均）を表示。5週未満は「データ不足」。

### 実装中の機能

- 実機でのプロフィール保存、負荷計算、分析表示の手動確認。

### 未着手の機能

- 実施履歴そのものの一覧・詳細閲覧（現状は取消と分析集計のみ）。
- 設定履歴の変更前後差分表示。
- 週メニュー切替の「今週から／次週月曜から」予約。
- JSONバックアップ／復元、インターバルタイマー。

## 3. 現在の作業内容

- 最後に完了した課題: 実運用フィードバックのUI・集計変更を実装し、本番ビルドを確認した。GitHubへのpush待ち。
- 現在発生している問題: GitHub Pagesを選択済み。公開先は`https://github.com/kzsakato/my-app.git`。ローカルGitリポジトリは初期化済みだが、Codex実行環境が`.git`へのロックファイル作成を拒否するため、Codexからコミット・pushできない。ユーザー自身のPowerShellでGit操作を行う必要がある。
- 現在発生している問題: 自動テストは未作成。計算結果とスマホ表示は実データで手動確認が必要。
- 注意: v3保存データをv4へ移行した時点を`analysisStartDate`に設定する。既存のテスト実績は分析対象から除外される。

## 4. 重要な設計上の決定

- 技術: TypeScript + React + Vite + vite-plugin-pwa + idb。Playストア用ネイティブアプリではない。
- 保存: IndexedDB `training-check` の `state/app`。スキーマはv4。
- 関係: 種目 → 実施項目 → 週メニュー所属。カテゴリは表示／分析のまとまり。
- 実施履歴には重量・回数・時間・セット数・実行時体重をスナップショット保存する。一方、部位・カテゴリ・種目の分類は現在の設定を参照するため、分類修正は過去の分析にも反映される。
- 実施総重量: 回数型の外部重量 × 重量係数 × 回数 × セット数。
- 総トレーニング負荷（参考）: 実施総重量に、自重換算（体重×自重換算係数）と時間型（体重×秒間負荷係数×秒数）を加えた参考指標。時間型の数値はkgではなく負荷ポイント。
- 削除は完全削除でなく非表示化。履歴・分析を壊さない。
- 日付は端末のローカル日付、週は月曜始まり。

## 5. 重要なファイル

| パス | 役割 | 現在の状態 |
|---|---|---|
| `src/App.tsx` | 画面、設定、実行、集計・折れ線グラフ、JSON入出力 | v5画面変更まで実装済み。 |
| `src/data.ts` | 型、初期データ、IndexedDB、スキーマ移行 | v4→v5移行を実装済み。 |
| `src/styles.css` | スマホ優先スタイル、分析グラフ | 分析表示用スタイルを追加済み。 |
| `.github/workflows/deploy-pages.yml` | GitHub Pages公開用GitHub Actions | 追加済み。公開リポジトリへのpush後に実行される。 |
| `.gitignore` | Gitで追跡しない生成物 | `node_modules`、`dist`等を対象に追加済み。 |
| `PROJECT_STATUS.md` | 再開用の状態記録 | この内容に更新済み。 |

## 6. 動作確認

- 実行済み: `node_modules\typescript\bin\tsc -b`（成功）、`node_modules\vite\bin\vite.js build --configLoader runner`（成功）。2026-09-09、JSON入出力・実行取消変更後。
- 確認できたこと: PWA本番成果物が`dist/`へ生成される。
- 未確認: Android実機でプロフィール保存、種目係数、実行後のトップ指標、全期間切替と各グラフの表示・計算。
- 既知の不具合: なし。

## 7. 次にやるべきこと

1. `src/App.tsx` の `valueOf`、`LoadSummary`、`Analysis` を確認する。
2. ユーザー自身のPowerShellで、`git add src/App.tsx src/data.ts src/styles.css vite.config.ts PROJECT_STATUS.md`、`git commit -m "Improve weekly training workflow"`、`git push`を実行する。GitHub Actionsの成功後、`https://kzsakato.github.io/my-app/`で更新を確認する。

3. 次を実行してPCまたはLAN上のスマホで最終確認する。

   ```powershell
   Set-Location 'C:\Users\kzsak\Documents\Codex\2026-09-03\new-chat'
   $taskNode='C:\Users\kzsak\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe'
   & $taskNode .\node_modules\vite\bin\vite.js --configLoader runner --host 0.0.0.0
   ```

4. 設定 → データ管理でバックアップを作成し、端末へJSONが保存されることを確認する。テスト用のバックアップを復元し、確認ダイアログ後に状態が戻ることを確認する。
5. 設定 → プロフィールで体重を確認する。種目で回数型の自重換算係数、時間型の秒間負荷係数を設定し、実施を完了する。
6. トップ最下部の今週負荷、平均、ACWRを確認する。設定 → 履歴・分析で単位・表示・集計の切替とグラフ縦軸の上／中央／0を確認する。
